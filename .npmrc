10.9.4

# pnpm configuration
node-linker=hoisted
auto-install-peers=true
strict-peer-dependencies=false

# Allow build scripts for native modules (needed for appdmg dependencies)
# This allows macos-alias, fs-xattr, and other native modules to run node-gyp
enable-pre-post-scripts=true
