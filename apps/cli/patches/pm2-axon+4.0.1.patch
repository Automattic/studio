diff --git a/node_modules/pm2-axon/lib/sockets/sock.js b/node_modules/pm2-axon/lib/sockets/sock.js
index 0d01379..1c53b73 100644
--- a/node_modules/pm2-axon/lib/sockets/sock.js
+++ b/node_modules/pm2-axon/lib/sockets/sock.js
@@ -8,7 +8,7 @@ var Configurable = require('../configurable');
 var debug = require('debug')('axon:sock');
 var Message = require('amp-message');
 var Parser = require('amp').Stream;
-var url = require('url');
+var URL = require('node:url').URL;
 var net = require('net');
 var fs = require('fs');
 
@@ -33,6 +33,17 @@ var ignore = [
 
 module.exports = Socket;
 
+/**
+ * Parse endpoint value as URL when valid, otherwise treat as unix socket path.
+ *
+ * @param {String} value
+ * @return {URL|Object}
+ */
+function parseEndpoint(value) {
+  if (URL.canParse(value)) return new URL(value);
+  return { pathname: value };
+}
+
 /**
  * Initialize a new `Socket`.
  *
@@ -245,9 +256,9 @@ Socket.prototype.connect = function(port, host, fn){
   if ('function' == typeof host) fn = host, host = undefined;
 
   if ('string' == typeof port) {
-    port = url.parse(port);
+    port = parseEndpoint(port);
 
-    if (port.pathname) {
+    if (!port.hostname || !port.port) {
       fn = host;
       host = null;
       fn = undefined;
@@ -349,9 +360,9 @@ Socket.prototype.bind = function(port, host, fn){
   var unixSocket = false;
 
   if ('string' == typeof port) {
-    port = url.parse(port);
+    port = parseEndpoint(port);
 
-    if (port.pathname) {
+    if (!port.hostname || !port.port) {
       fn = host;
       host = null;
       port = port.pathname;
