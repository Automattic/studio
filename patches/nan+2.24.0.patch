diff --git a/node_modules/nan/nan.h b/node_modules/nan/nan.h
index b9d191a..baa4ce8 100644
--- a/node_modules/nan/nan.h
+++ b/node_modules/nan/nan.h
@@ -105,6 +105,14 @@
 # endif
 #endif
 
+// Compatibility shim: v8::AccessControl was removed in V8 >= 12.4
+#if defined(V8_MAJOR_VERSION) && (V8_MAJOR_VERSION > 12 || \
+    (V8_MAJOR_VERSION == 12 && defined(V8_MINOR_VERSION) && V8_MINOR_VERSION >= 4))
+namespace v8 {
+  enum AccessControl { DEFAULT = 0 };
+}
+#endif
+
 namespace Nan {
 
 #define NAN_CONCAT(a, b) NAN_CONCAT_HELPER(a, b)
