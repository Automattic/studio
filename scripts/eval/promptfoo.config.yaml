# Studio Code Agent Evaluation
# Run: npm run eval          (builds CLI first)
# View: npm run eval:view
#
# The runner writes its payload to a tmp file and prints only
# `EVAL_RUNNER_RESULT_FILE=/path/to.json` on stdout. Assertions resolve the
# marker via `import('node:fs').then(...)` — not `require('fs')`, which is
# undefined inside promptfoo's assertion eval context.

description: Studio Code agent evaluation

# Tests create/delete real local sites; running them in parallel makes two
# runs fight over ports and on-disk state.
evaluateOptions:
  maxConcurrency: 1

providers:
  - id: exec:node ../../apps/cli/dist/cli/eval-runner.mjs
    label: studio-agent

# Custom grader uses Studio's WP.com auth for llm-rubric assertions.
defaultTest:
  options:
    provider: file://grader-provider.mjs
    repeat: 1

tests:
  - description: agent identifies as WordPress Studio AI
    vars:
      caseId: identity
      timeoutMs: 60000
      prompt: In one short sentence, tell me who you are. Do not call any tools.
    assert:
      - type: javascript
        value: |
          return import('node:fs').then(({ readFileSync }) => {
            const marker = output.split(/\r?\n/).map(l => l.trim()).find(l => l.startsWith('EVAL_RUNNER_RESULT_FILE='));
            if (!marker) return { pass: false, score: 0, reason: `no result-file marker on stdout; got: ${output.slice(0, 200)}` };
            const d = JSON.parse(readFileSync(marker.slice('EVAL_RUNNER_RESULT_FILE='.length), 'utf8'));
            if (d.success !== true) return { pass: false, score: 0, reason: `runner success=${d.success}` };
            const text = (d.textSegments || []).join('\n');
            const mentionsStudio = /WordPress\s+Studio/i.test(text);
            const claimsOther = /\bI\s+am\s+(Claude|ChatGPT|a large language model|an AI (model|assistant) (made|built|developed) by Anthropic)\b/i.test(text);
            const pass = mentionsStudio && !claimsOther;
            return {
              pass,
              score: pass ? 1 : 0,
              reason: pass ? 'identifies as WordPress Studio' : `got: ${text.slice(0, 300)}`,
            };
          });

  - description: agent calls site_create when asked to make a site
    vars:
      caseId: site-creation
      timeoutMs: 120000
      askUserPolicy: allow_all
      prompt: |
        First check if a site named "Eval Test Site" exists using site_list.
        If it does, delete it with site_delete. Then create a new site named "Eval Test Site".
    assert:
      - type: javascript
        value: |
          return import('node:fs').then(({ readFileSync }) => {
            const marker = output.split(/\r?\n/).map(l => l.trim()).find(l => l.startsWith('EVAL_RUNNER_RESULT_FILE='));
            if (!marker) return { pass: false, score: 0, reason: `no result-file marker on stdout; got: ${output.slice(0, 200)}` };
            const d = JSON.parse(readFileSync(marker.slice('EVAL_RUNNER_RESULT_FILE='.length), 'utf8'));
            const called = d.toolCalls?.some(t => t.name === 'site_create');
            return { pass: !!called, score: called ? 1 : 0, reason: called ? 'site_create was called' : `no site_create in ${JSON.stringify(d.toolCalls?.map(t => t.name) ?? [])}` };
          });
      - type: javascript
        value: |
          return import('node:fs').then(({ readFileSync }) => {
            const marker = output.split(/\r?\n/).map(l => l.trim()).find(l => l.startsWith('EVAL_RUNNER_RESULT_FILE='));
            if (!marker) return { pass: false, score: 0, reason: `no result-file marker on stdout; got: ${output.slice(0, 200)}` };
            const d = JSON.parse(readFileSync(marker.slice('EVAL_RUNNER_RESULT_FILE='.length), 'utf8'));
            const result = d.toolResults?.find(r => r.toolName === 'site_create');
            const ok = !!(result && !result.isError);
            return { pass: ok, score: ok ? 1 : 0, reason: ok ? 'site_create succeeded' : `site_create result: ${JSON.stringify(result)}` };
          });

  # Regression: trunk handles desktop/mobile visual verification as separate
  # screenshot calls. The optimized tool exposes `viewport: "all"` so the agent
  # can satisfy the same user request with one timed call and two PNG payloads.
  - description: screenshot verification captures desktop and mobile in one fast call
    vars:
      caseId: screenshot-all-timing
      timeoutMs: 240000
      askUserPolicy: allow_all
      prompt: |
        First check if a site named "Eval Screenshot Timing" exists using
        site_list. If it does, delete it with site_delete so we start from
        a clean slate. Do NOT touch any other site.

        Then create a new site named "Eval Screenshot Timing". Do not
        customize the theme. Verify the homepage visually with screenshots
        for both desktop and mobile, then briefly report whether the
        screenshots were captured.
    assert:
      - type: javascript
        value: |
          return import('node:fs').then(({ readFileSync, existsSync }) => {
            const marker = output.split(/\r?\n/).map(l => l.trim()).find(l => l.startsWith('EVAL_RUNNER_RESULT_FILE='));
            if (!marker) return { pass: false, score: 0, reason: `no result-file marker on stdout; got: ${output.slice(0, 200)}` };
            const d = JSON.parse(readFileSync(marker.slice('EVAL_RUNNER_RESULT_FILE='.length), 'utf8'));
            const screenshotCalls = (d.toolCalls || []).filter(t => t.name === 'take_screenshot');
            if (screenshotCalls.length !== 1) {
              return {
                pass: false,
                score: 0,
                reason: `expected one take_screenshot call for desktop+mobile verification, got ${screenshotCalls.length}: ${JSON.stringify(screenshotCalls.map(t => t.input))}`,
              };
            }
            const call = screenshotCalls[0];
            if (call.input?.viewport !== 'all') {
              return {
                pass: false,
                score: 0,
                reason: `expected take_screenshot to use viewport="all"; got ${JSON.stringify(call.input)}`,
              };
            }

            const result = (d.toolResults || []).find(r => r.toolUseId === call.id);
            if (!result || result.isError) {
              return { pass: false, score: 0, reason: `take_screenshot failed: ${JSON.stringify(result)}` };
            }
            const rawPayloads = (result.text || '').split('mediaWidgetPayloads=')[1];
            if (!rawPayloads) {
              return { pass: false, score: 0, reason: `screenshot result did not include mediaWidgetPayloads: ${(result.text || '').slice(0, 500)}` };
            }

            let payloads;
            try {
              payloads = JSON.parse(rawPayloads);
            } catch (error) {
              return { pass: false, score: 0, reason: `could not parse mediaWidgetPayloads JSON: ${error instanceof Error ? error.message : String(error)}` };
            }
            if (!Array.isArray(payloads) || payloads.length !== 2) {
              return { pass: false, score: 0, reason: `expected two screenshot media payloads, got ${JSON.stringify(payloads).slice(0, 500)}` };
            }

            // take_screenshot emits JPEG; key the expected width by viewport so
            // the check survives a future format change rather than the extension.
            const expectedWidths = new Map([
              ['desktop', 1040],
              ['mobile', 390],
            ]);
            // Read pixel dimensions from a PNG or JPEG buffer; null if neither.
            const readImageSize = (buf) => {
              if (buf.length > 24 && buf[0] === 0x89 && buf[1] === 0x50 && buf[2] === 0x4e && buf[3] === 0x47) {
                return { format: 'png', width: buf.readUInt32BE(16), height: buf.readUInt32BE(20) };
              }
              if (buf.length > 4 && buf[0] === 0xff && buf[1] === 0xd8) {
                let offset = 2;
                while (offset + 9 < buf.length) {
                  if (buf[offset] !== 0xff) { offset++; continue; }
                  const marker = buf[offset + 1];
                  // Markers without a length payload: skip past them.
                  if (marker === 0xd8 || marker === 0xd9 || (marker >= 0xd0 && marker <= 0xd7) || marker === 0x01) {
                    offset += 2;
                    continue;
                  }
                  // Start-of-frame markers carry the dimensions (excluding DHT/JPG/DAC).
                  if (marker >= 0xc0 && marker <= 0xcf && marker !== 0xc4 && marker !== 0xc8 && marker !== 0xcc) {
                    return { format: 'jpeg', height: buf.readUInt16BE(offset + 5), width: buf.readUInt16BE(offset + 7) };
                  }
                  offset += 2 + buf.readUInt16BE(offset + 2);
                }
              }
              return null;
            };
            const seen = [];
            for (const payload of payloads) {
              const source = payload.widgetProps?.source;
              const viewport = (source?.name || '').replace(/^screenshot-/, '').replace(/\.(png|jpe?g)$/i, '');
              const expectedWidth = expectedWidths.get(viewport);
              if (!expectedWidth) {
                return { pass: false, score: 0, reason: `unexpected screenshot payload source: ${JSON.stringify(source)}` };
              }
              if (typeof source.path !== 'string' || !existsSync(source.path)) {
                return { pass: false, score: 0, reason: `screenshot file is missing: ${JSON.stringify(source)}` };
              }
              const size = readImageSize(readFileSync(source.path));
              if (!size || size.width !== expectedWidth || size.height < 600) {
                return {
                  pass: false,
                  score: 0,
                  reason: `${source.name} did not look like the expected screenshot: ${JSON.stringify(size)}`,
                };
              }
              seen.push(source.name);
            }
            return { pass: true, score: 1, reason: `captured valid screenshots: ${seen.join(', ')}` };
          });
      - type: javascript
        value: |
          return import('node:fs').then(({ readFileSync }) => {
            const marker = output.split(/\r?\n/).map(l => l.trim()).find(l => l.startsWith('EVAL_RUNNER_RESULT_FILE='));
            if (!marker) return { pass: false, score: 0, reason: `no result-file marker on stdout; got: ${output.slice(0, 200)}` };
            const d = JSON.parse(readFileSync(marker.slice('EVAL_RUNNER_RESULT_FILE='.length), 'utf8'));
            const events = (d.toolEvents || []).filter(t => t.toolName === 'take_screenshot');
            if (events.length !== 1 || typeof events[0].durationMs !== 'number') {
              return { pass: false, score: 0, reason: `expected one timed take_screenshot event, got ${JSON.stringify(events)}` };
            }
            const limitMs = 15000;
            const duration = events[0].durationMs;
            const pass = duration < limitMs;
            return {
              pass,
              score: pass ? 1 : 0,
              reason: pass
                ? `take_screenshot completed in ${duration}ms`
                : `take_screenshot took ${duration}ms, expected < ${limitMs}ms`,
            };
          });

  # Every individual turn (wall-clock between successive assistant messages)
  # should stay under 60s. Slow turns stall the UI.
  - description: single-page site build keeps every turn under 60s
    vars:
      caseId: single-page-build-turn-cadence
      timeoutMs: 600000
      askUserPolicy: allow_all
      prompt: |
        First check if a site named "Eval Turn Timing" exists using
        site_list. If it does, delete it with site_delete so we start from
        a clean slate. Do NOT touch any other site.

        Then build a simple WordPress site named "Eval Turn Timing". The
        site only needs a single page — no extra pages, no blog posts, no
        navigation menu beyond what a one-page site requires.
    assert:
      - type: javascript
        value: |
          return import('node:fs').then(({ readFileSync }) => {
            const marker = output.split(/\r?\n/).map(l => l.trim()).find(l => l.startsWith('EVAL_RUNNER_RESULT_FILE='));
            if (!marker) return { pass: false, score: 0, reason: `no result-file marker on stdout; got: ${output.slice(0, 200)}` };
            const d = JSON.parse(readFileSync(marker.slice('EVAL_RUNNER_RESULT_FILE='.length), 'utf8'));
            const durations = d.turnDurationsMs ?? [];
            if (durations.length === 0) {
              return { pass: false, score: 0, reason: 'no turns recorded' };
            }
            const max = Math.max(...durations);
            const maxIdx = durations.indexOf(max);
            if (max >= 60000) {
              return { pass: false, score: 0, reason: `turn ${maxIdx + 1}/${durations.length} took ${max}ms (>= 60000ms). All turns (ms): ${durations.join(', ')}` };
            }
            return { pass: true, score: 1, reason: `max turn ${max}ms across ${durations.length} turns` };
          });
      # Regression: the "Page content" cadence used to tell the agent to apply
      # page blocks via `wp_cli post update --post_content-file=<host path>`.
      # `wp_cli` runs inside the PHP-WASM filesystem, so host paths silently
      # resolve to empty content and the post is overwritten with nothing.
      # The fix is `wp_cli eval` + `file_get_contents(ABSPATH . ...)` — this
      # assertion fails if any wp_cli call reintroduces --post_content-file=.
      - type: javascript
        value: |
          return import('node:fs').then(({ readFileSync }) => {
            const marker = output.split(/\r?\n/).map(l => l.trim()).find(l => l.startsWith('EVAL_RUNNER_RESULT_FILE='));
            if (!marker) return { pass: false, score: 0, reason: `no result-file marker on stdout; got: ${output.slice(0, 200)}` };
            const d = JSON.parse(readFileSync(marker.slice('EVAL_RUNNER_RESULT_FILE='.length), 'utf8'));
            const offending = (d.toolCalls || []).filter(t => {
              if (t.name !== 'wp_cli') return false;
              const cmd = t.input && typeof t.input.command === 'string' ? t.input.command : '';
              return cmd.includes('--post_content-file=');
            });
            if (offending.length > 0) {
              const sample = JSON.stringify(offending[0].input).slice(0, 300);
              return { pass: false, score: 0, reason: `${offending.length} wp_cli call(s) used --post_content-file=, which silently fails inside PHP-WASM (first: ${sample})` };
            }
            return { pass: true, score: 1, reason: 'no wp_cli call used --post_content-file=' };
          });
      - type: javascript
        value: |
          return import('node:fs').then(({ readFileSync }) => {
            const marker = output.split(/\r?\n/).map(l => l.trim()).find(l => l.startsWith('EVAL_RUNNER_RESULT_FILE='));
            if (!marker) return { pass: false, score: 0, reason: `no result-file marker on stdout; got: ${output.slice(0, 200)}` };
            const d = JSON.parse(readFileSync(marker.slice('EVAL_RUNNER_RESULT_FILE='.length), 'utf8'));
            const validationResults = (d.toolResults || []).filter(r => r.toolName === 'validate_blocks');
            if (validationResults.length === 0) {
              return { pass: false, score: 0, reason: 'validate_blocks was not called for block content' };
            }
            const last = validationResults[validationResults.length - 1];
            const text = last.text || '';
            // validate_blocks runs the core/html policy gate first; only when it
            // passes does the live-editor validation run and emit `Validation:`.
            // A short-circuited result reports `N/M core/html blocks invalid`.
            const htmlPassed = /HTML block policy:/i.test(text) && !/core\/html blocks invalid/i.test(text);
            if (last.isError || !htmlPassed) {
              return {
                pass: false,
                score: 0,
                reason: `final validate_blocks result did not pass the core/html policy gate: ${(text || JSON.stringify(last)).slice(0, 500)}`,
              };
            }
            const failed =
              !/Validation:\s+\d+\/\d+\s+blocks valid/i.test(text) ||
              /Auto-fix proposal failed validation|Auto-fix proposal still has|No automatic editor serialization fix was available/i.test(text);
            if (failed) {
              return {
                pass: false,
                score: 0,
                reason: `final validate_blocks editor validation was invalid or inconclusive: ${(text || JSON.stringify(last)).slice(0, 500)}`,
              };
            }
            return { pass: true, score: 1, reason: `validation passed: ${text.split(/\r?\n/).filter(Boolean).slice(0, 2).join(' | ')}` };
          });

  # Regression (STU-1775): the agent used to present a temporary preview site as
  # a connected WordPress.com remote site when listing what a local site is
  # connected to. We seed one connected remote site and one preview site for a
  # local site, then assert (a) each tool tags its output with a `type`
  # discriminator and (b) the agent's prose keeps the two categories distinct.
  - description: agent distinguishes preview sites from connected WordPress.com remote sites
    vars:
      caseId: differentiate-preview-vs-remote
      timeoutMs: 180000
      askUserPolicy: allow_all
      seed:
        localSite:
          id: eval-site-categories
          name: Eval Site Categories
          path: /tmp/eval-site-categories
          port: 8881
        connectedWpcomSites:
          - id: 111
            localSiteId: eval-site-categories
            name: My Production Site
            url: https://myprod.wordpress.com
            isStaging: false
            isPressable: false
            environmentType: production
            syncSupport: already-connected
            lastPullTimestamp: null
            lastPushTimestamp: null
        snapshots:
          - url: abc123-studio.wp.build
            atomicSiteId: 111
            localSiteId: eval-site-categories
            date: 1900000000000
            name: Eval Site Categories
      prompt: |
        For the local site named "Eval Site Categories", list everything it is
        connected or related to. For each item, tell me clearly whether it is a
        temporary PREVIEW site or a durable connected WordPress.com remote site.
        Do not create, delete, or modify anything — only inspect and report.
    assert:
      # The connected-remote tool output carries the type discriminator.
      - type: javascript
        value: |
          return import('node:fs').then(({ readFileSync }) => {
            const marker = output.split(/\r?\n/).map(l => l.trim()).find(l => l.startsWith('EVAL_RUNNER_RESULT_FILE='));
            if (!marker) return { pass: false, score: 0, reason: `no result-file marker on stdout; got: ${output.slice(0, 200)}` };
            const d = JSON.parse(readFileSync(marker.slice('EVAL_RUNNER_RESULT_FILE='.length), 'utf8'));
            const r = (d.toolResults || []).find(r => r.toolName === 'site_connected_remote_sites');
            if (!r) return { pass: false, score: 0, reason: `site_connected_remote_sites was not called; tools: ${JSON.stringify((d.toolCalls || []).map(t => t.name))}` };
            const ok = /"type"\s*:\s*"wpcom-remote"/.test(r.text || '');
            return { pass: ok, score: ok ? 1 : 0, reason: ok ? 'connected-remote output tagged type=wpcom-remote' : `missing type discriminator: ${(r.text || '').slice(0, 300)}` };
          });
      # The preview tool output carries the type discriminator.
      - type: javascript
        value: |
          return import('node:fs').then(({ readFileSync }) => {
            const marker = output.split(/\r?\n/).map(l => l.trim()).find(l => l.startsWith('EVAL_RUNNER_RESULT_FILE='));
            if (!marker) return { pass: false, score: 0, reason: `no result-file marker on stdout; got: ${output.slice(0, 200)}` };
            const d = JSON.parse(readFileSync(marker.slice('EVAL_RUNNER_RESULT_FILE='.length), 'utf8'));
            const r = (d.toolResults || []).find(r => r.toolName === 'preview_list');
            if (!r) return { pass: false, score: 0, reason: `preview_list was not called; tools: ${JSON.stringify((d.toolCalls || []).map(t => t.name))}` };
            const ok = /"type"\s*:\s*"preview"/.test(r.text || '');
            return { pass: ok, score: ok ? 1 : 0, reason: ok ? 'preview output tagged type=preview' : `missing type discriminator: ${(r.text || '').slice(0, 300)}` };
          });
      # The behavioral guard: the agent must not merge the two categories.
      # Grade the assistant's prose, not the EVAL_RUNNER_RESULT_FILE marker line.
      - type: llm-rubric
        transform: |
          return import('node:fs').then(({ readFileSync }) => {
            const marker = String(output).split(/\r?\n/).map(l => l.trim()).find(l => l.startsWith('EVAL_RUNNER_RESULT_FILE='));
            if (!marker) return String(output);
            const d = JSON.parse(readFileSync(marker.slice('EVAL_RUNNER_RESULT_FILE='.length), 'utf8'));
            return (d.textSegments || []).join('\n');
          });
        value: |
          The assistant lists what the local site "Eval Site Categories" is
          connected or related to. PASS only if it classifies
          "abc123-studio.wp.build" as a temporary/preview site (and NOT as a
          connected WordPress.com remote/production site), AND classifies
          "My Production Site" / "myprod.wordpress.com" as a connected
          WordPress.com remote site (and NOT as a preview site). FAIL if it
          labels the preview as a "WordPress.com" remote/production site, labels
          the remote site as a preview, or merges the two into one undifferentiated list.

  - description: agent reaches for Jetpack on slideshow request (catch-all rule)
    vars:
      caseId: jetpack-catchall-slideshow
      timeoutMs: 600000
      askUserPolicy: allow_all
      prompt: |
        First check if a site named "Catchall Eval Site" exists using
        site_list. If it does, delete it with site_delete so we start
        from a clean slate. Do NOT touch any other site.

        Then create a new site named "Catchall Eval Site" and add a
        slideshow of three placeholder images to its homepage.
    assert:
      - type: javascript
        value: |
          return import('node:fs').then(({ readFileSync }) => {
            const marker = output.split(/\r?\n/).map(l => l.trim()).find(l => l.startsWith('EVAL_RUNNER_RESULT_FILE='));
            if (!marker) return { pass: false, score: 0, reason: `no result-file marker on stdout; got: ${output.slice(0, 200)}` };
            const d = JSON.parse(readFileSync(marker.slice('EVAL_RUNNER_RESULT_FILE='.length), 'utf8'));
            const writtenContents = (d.toolCalls || [])
              .filter(c => c.name === 'Write' && c.input && typeof c.input.content === 'string')
              .map(c => c.input.content);
            const usedJetpackBlock = writtenContents.some(c => /<!--\s*wp:jetpack\//.test(c));
            return {
              pass: usedJetpackBlock,
              score: usedJetpackBlock ? 1 : 0,
              reason: usedJetpackBlock
                ? 'page content uses a jetpack/* block'
                : `no jetpack/* block in any Write tool call; sample: ${(writtenContents[0] || '').slice(0, 300)}`,
            };
          });

  # Regression: a "shop"/"store" request must set up WooCommerce WITH products,
  # even when framed with scope/style qualifiers ("small", "for inspiration")
  # that the agent previously used to misclassify the site as informational and
  # skip ecommerce. Guards both the proactive plugin-recommendations trigger and
  # the ecommerce-by-default classification.
  - description: shop request sets up WooCommerce with products
    vars:
      caseId: woocommerce-shop-setup
      timeoutMs: 600000
      askUserPolicy: allow_all
      prompt: |
        First check if a site named "Eval Shop Site" exists using site_list.
        If it does, delete it with site_delete so we start from a clean
        slate. Do NOT touch any other site.

        Then build a small pet shop site named "Eval Shop Site" — keep it
        small, just a couple of pages, look at a clean modern store for
        inspiration. I want to sell dog and cat products.
    assert:
      - type: javascript
        value: |
          return import('node:fs').then(({ readFileSync }) => {
            const marker = output.split(/\r?\n/).map(l => l.trim()).find(l => l.startsWith('EVAL_RUNNER_RESULT_FILE='));
            if (!marker) return { pass: false, score: 0, reason: `no result-file marker on stdout; got: ${output.slice(0, 200)}` };
            const d = JSON.parse(readFileSync(marker.slice('EVAL_RUNNER_RESULT_FILE='.length), 'utf8'));
            const installed = (d.toolCalls || []).some(t => t.name === 'wp_cli' && /plugin\s+install\s+woocommerce/.test(t.input?.command || ''));
            return { pass: installed, score: installed ? 1 : 0, reason: installed ? 'WooCommerce installed' : `no woocommerce install; wp_cli cmds: ${JSON.stringify((d.toolCalls || []).filter(t => t.name === 'wp_cli').map(t => t.input?.command))}` };
          });
      - type: javascript
        value: |
          return import('node:fs').then(({ readFileSync }) => {
            const marker = output.split(/\r?\n/).map(l => l.trim()).find(l => l.startsWith('EVAL_RUNNER_RESULT_FILE='));
            if (!marker) return { pass: false, score: 0, reason: `no result-file marker on stdout; got: ${output.slice(0, 200)}` };
            const d = JSON.parse(readFileSync(marker.slice('EVAL_RUNNER_RESULT_FILE='.length), 'utf8'));
            const n = (d.toolCalls || []).filter(t => t.name === 'wp_cli' && /wc\s+product\s+create/.test(t.input?.command || '')).length;
            return { pass: n >= 1, score: n >= 1 ? 1 : 0, reason: n >= 1 ? `${n} product(s) created` : 'no `wc product create` calls' };
          });

  # When adding sections to a site that already has an active theme, the agent
  # should color them from the theme's color palette (block color-slug
  # attributes like {"backgroundColor":"accent-1"} or var(--wp--preset--color--*)
  # in CSS) instead of hardcoding hex values. Hardcoded hex detaches the section
  # from Global Styles / theme switching / dark variations and is the behavior
  # this case guards against. theme.json is excluded from the hex check because
  # the palette is legitimately *defined* there with hex.
  - description: added sections use the theme color palette, not hardcoded hex
    vars:
      caseId: section-uses-theme-palette
      timeoutMs: 600000
      askUserPolicy: allow_all
      prompt: |
        First check if a site named "Eval Palette Site" exists using
        site_list. If it does, delete it with site_delete so we start from a
        clean slate. Do NOT touch any other site.

        Then create a new site named "Eval Palette Site". Keep its current
        default theme — do not create, scaffold, or switch themes.

        Add two visually distinct sections to the site's homepage: a hero
        section with a colored background and a heading, and a call-to-action
        section with a contrasting colored background and a button. Make the
        colors feel cohesive with the site's existing theme.
    assert:
      - type: javascript
        value: |
          return import('node:fs').then(({ readFileSync }) => {
            const marker = output.split(/\r?\n/).map(l => l.trim()).find(l => l.startsWith('EVAL_RUNNER_RESULT_FILE='));
            if (!marker) return { pass: false, score: 0, reason: `no result-file marker on stdout; got: ${output.slice(0, 200)}` };
            const d = JSON.parse(readFileSync(marker.slice('EVAL_RUNNER_RESULT_FILE='.length), 'utf8'));
            const calls = d.toolCalls || [];
            const pathOf = (i) => String((i && (i.file_path || i.path || i.filePath)) || '').toLowerCase();
            // Collect every place a color can land. Block markup and theme CSS
            // reach the site through Write/Edit (a scratch .html, style.css, or
            // raw block markup) OR through wp_cli — both as post_content and as
            // CSS injected via `wp_cli eval '$css = "..."'`. theme.json is the one
            // exclusion: a palette is legitimately defined there with hex.
            const blobs = [];
            for (const c of calls) {
              const i = c.input || {};
              if ((c.name === 'Write' || c.name === 'Edit') && !pathOf(i).endsWith('.json')) {
                for (const k of ['content', 'new_string', 'replacement', 'text']) {
                  if (typeof i[k] === 'string') blobs.push(i[k]);
                }
                // The Edit tool applies changes as { edits: [{ oldText, newText }] }.
                if (Array.isArray(i.edits)) {
                  for (const e of i.edits) if (e && typeof e.newText === 'string') blobs.push(e.newText);
                }
              }
              if (c.name === 'wp_cli' && typeof i.command === 'string') {
                blobs.push(i.command);
              }
            }
            const markup = blobs.join('\n');
            if (!/<!--\s*wp:/.test(markup)) {
              return { pass: false, score: 0, reason: `no block markup found in Write/wp_cli calls; tool calls: ${JSON.stringify(calls.map(c => c.name))}` };
            }
            // Hardcoded hex colors in section markup / theme CSS = custom colors (the bug).
            const hex = markup.match(/#[0-9a-fA-F]{6}\b|#[0-9a-fA-F]{3}\b/g) || [];
            // Palette references: block color-slug attributes or preset CSS vars.
            const slugAttr = markup.match(/"(?:backgroundColor|textColor|borderColor|overlayColor|gradient)"\s*:\s*"[a-z][a-z0-9-]*"/g) || [];
            const presetVar = markup.match(/var\(\s*--wp--preset--color--[a-z0-9-]+/g) || [];
            const paletteRefs = slugAttr.length + presetVar.length;
            if (hex.length > 0) {
              const uniq = [...new Set(hex)].slice(0, 8);
              return { pass: false, score: 0, reason: `section markup/CSS hardcodes ${hex.length} custom hex color(s) (${uniq.join(', ')}) instead of theme palette colors; palette refs found: ${paletteRefs}` };
            }
            if (paletteRefs === 0) {
              return { pass: false, score: 0, reason: `sections apply no theme-palette colors (no color-slug attributes or --wp--preset--color-- vars) — cannot confirm the section is colored from the palette` };
            }
            return { pass: true, score: 1, reason: `sections colored from the theme palette (${paletteRefs} palette color ref(s)) with no hardcoded hex` };
          });

  # A single-fact question must be answered in chat prose, without pinning a
  # desks widget. `forceChatArtifacts` registers studio_present headless so
  # the presentation rules can be evaluated. Guards the smallest-useful-widget
  # presentation rule.
  - description: single-fact question is answered in prose without presenting widgets
    vars:
      caseId: single-fact-no-widgets
      timeoutMs: 180000
      askUserPolicy: allow_all
      forceChatArtifacts: true
      seed:
        localSite:
          id: eval-widget-site
          name: Eval Widget Site
          path: /tmp/eval-widget-site
          port: 8899
      prompt: |
        Tell me the local URL of my site "Eval Widget Site".
    assert:
      - type: javascript
        value: |
          return import('node:fs').then(({ readFileSync }) => {
            const marker = output.split(/\r?\n/).map(l => l.trim()).find(l => l.startsWith('EVAL_RUNNER_RESULT_FILE='));
            if (!marker) return { pass: false, score: 0, reason: `no result-file marker on stdout; got: ${output.slice(0, 200)}` };
            const d = JSON.parse(readFileSync(marker.slice('EVAL_RUNNER_RESULT_FILE='.length), 'utf8'));
            const presents = (d.toolCalls || []).filter(c => c.name === 'studio_present');
            if (presents.length > 0) {
              return { pass: false, score: 0, reason: `single-fact question produced ${presents.length} studio_present call(s): ${JSON.stringify(presents.map(p => p.input)).slice(0, 300)}` };
            }
            return { pass: true, score: 1, reason: 'no widgets presented for a single-fact question' };
          });
      - type: javascript
        value: |
          return import('node:fs').then(({ readFileSync }) => {
            const marker = output.split(/\r?\n/).map(l => l.trim()).find(l => l.startsWith('EVAL_RUNNER_RESULT_FILE='));
            if (!marker) return { pass: false, score: 0, reason: `no result-file marker on stdout; got: ${output.slice(0, 200)}` };
            const d = JSON.parse(readFileSync(marker.slice('EVAL_RUNNER_RESULT_FILE='.length), 'utf8'));
            const text = (d.textSegments || []).join('\n');
            const answered = /localhost:8899/.test(text);
            return { pass: answered, score: answered ? 1 : 0, reason: answered ? 'URL answered in prose' : `prose did not contain the URL; got: ${text.slice(0, 300)}` };
          });
